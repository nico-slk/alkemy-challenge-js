import React, { useReducer } from 'react'

const AccountState = () => {
    const accountInitialState = {
        history: []
    }

    const getHistory = () => { }

    const editHistory = () => { }
}

export default AccountState;